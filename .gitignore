# Visual Studio
Release
bin
obj
*.ncb
*.vcproj*
*.suo
*.user
*.sln.cache
*.manifest
*.CodeAnalysisLog.xml
*.lastcodeanalysissucceeded
_ReSharper.*/
/.vs

# Visual Studio Code
/.vscode/settings.json

# binaries
IP2LOCATION-LITE-DB1.IPV6.BIN.ZIP

# backup files by various editors
*~
*.orig
\#*
.*.sw?

# Mac OS X
.DS_Store

# auto-generated documentation
DOCUMENTATION.md
WEAPONS.md
Lua-API.md
Settings.md
*.html
# ...but keep the GitHub Pages site (docs/) — it is hand-written, not auto-generated
!docs/*.html
!docs/**/*.html
# ...and the wasm probe's browser host page
!OpenRA.WasmProbe/wwwroot/*.html
openra.6
update.log

# SublimeText
*.sublime-project
*.sublime-workspace

# NUnit
/TestResult.xml
/lib/

# Support directory
/Support

# IntelliJ files
.idea

# Local .NET SDK / wasm-tools installs and OpenRA's own runtime support dir,
# both of which land in the repo root when tooling is fetched/run locally
# instead of in CI.
/.dotnet/
/.dotnet_home/
/Library/

# Ad hoc local test/debug output (CI uploads the real artifacts instead).
*.log
ai-test-timeout.png
ai-match-progress.png
playwasm-smoke-screenshot.png
